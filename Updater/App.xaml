<Application x:Class="Updater.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:custom="clr-namespace:Updater.CustomElements"
             StartupUri="Auth.xaml">
    <Application.Resources>
        <Style TargetType="custom:BuildDeployCountTextBox">
            <Setter Property="IsReadOnly" Value="True" />
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="#FFE5E5E5"/>
            <Setter Property="Margin" Value="0,0,0,0"/>
            <Setter Property="MaxWidth" Value="170"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
        </Style>
        <Style TargetType="custom:ChangesTextBox">
            <Setter Property="IsReadOnly" Value="True" />
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Margin" Value="10,0,0,0"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>
        <Style TargetType="custom:ChangeLogVersion">
            <Setter Property="TextDecorations" Value="Underline"/>
            <Setter Property="Margin" Value="5,5,0,5"/>
        </Style>
    </Application.Resources>
</Application>
